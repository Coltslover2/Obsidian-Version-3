export async function searchAppleEducation({ query, country, limit }) {
  // TODO: implement Apple EDU catalog matching (region + eligibility).
  // For V1: you can show MSRP + "eligible?" messaging, or only track public pricing.
  return [];
}
