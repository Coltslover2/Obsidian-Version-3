export async function searchNikeDrops({ query, country, limit }) {
  // TODO: implement Nike drop monitoring + restock alerts.
  // This is usually a separate pipeline from normal price comparison.
  return [];
}
